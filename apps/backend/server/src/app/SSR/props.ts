// import { getListItems, getMenuItems } from './db'

export function initProps() {
  return { 
    // staring params to app
    // listItems: getListItems(), 
    // menuItems: getMenuItems() 
  }
}
