import { Observable, ErrorObserver } from 'rxjs'
import { ListingService } from './listing'
import { AuthInitializationTypes as authTypes } from '../features-api'

export class VenueListingProvider extends ListingService {
  // constructor(api: 'api'|'admin', listingType?:string, auth?:authTypes) {
  //   super(api, listingType)
  //   this.authenticate(auth.headers)
  //     .then(a => console.log('Auth'))
  // }

  // create() {
    
  // }

  // fetchLazy(f, next) {

  // }

  // fetchFiltered() {
    
  // }
}